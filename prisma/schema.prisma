generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}

model sample {
  id   String @id @db.VarChar(100)
  name String @db.VarChar(100)
}
